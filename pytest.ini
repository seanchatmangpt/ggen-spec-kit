[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
markers =
    integration: Integration tests using testcontainers (slow)
    requires_docker: Tests that require Docker to be running

# Coverage options (requires pytest-cov to be installed)
# Uncomment after installing: uv pip install -e ".[test]"
# --cov=src
# --cov-report=term-missing
# --cov-report=html
